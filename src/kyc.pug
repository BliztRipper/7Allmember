extends _template
block body
  -var page = 'kyc'
  .header.d-flex.justify-content-center.border-bottom
    .sprite--logo
  section
    .container.d-flex.align-items-center.flex-column
      .sprite--allCard
      span.connectCard เชื่อมบัตร All Member Visa Card
      span.connectCard--detail กรุณากรอกข้อมูลเพิ่มเติม 
  section
    .container.form
      form.needs-validation(action='signing.html' method='' novalidate)
        .col-12.form-row
          label.form__label(for='validate-name') ชื่อ (ภาษาไทย)
          input.form-control#validate-name(type='text' placeholder='กิตติ' required)
          .valid-feedback 
          .invalid-feedback กรุณากรอกชื่อภาษาไทย
            .invalid-feedback__desc
        .col-12.form-row
          label.form__label(for='validate-surname') นามสกุล (ภาษาไทย)
          input.form-control#validate-surname(type='text' placeholder='สิงหาคม' required)
          .valid-feedback 
          .invalid-feedback กรุณากรอกนามสกุลภาษาไทย
            .invalid-feedback__desc
        .col-12.form-row
          label.form__label(for='validate-name-eng') ชื่อ (ภาษาอังกฤษ)
          input.form-control#validate-name-eng(type='text' placeholder='Kitti' required)
          .valid-feedback 
          .invalid-feedback กรุณากรอกชื่อภาษาอังกฤษ
            .invalid-feedback__desc
        .col-12.form-row
          label.form__label(for='validate-surname-eng') นามสกุล (ภาษาอังกฤษ)
          input.form-control#validate-surname-eng(type='text' placeholder='Singhakom' required)
          .valid-feedback 
          .invalid-feedback กรุณากรอกนามสกุลภาษาอังกฤษ
            .invalid-feedback__desc
        .col-12.form-row
          label.form__label(for='validate-dob') วัน / เดือน / พ.ศ.
          input.form-control#validate-dob(type='tel' placeholder='' required)
          .valid-feedback 
          .invalid-feedback กรุณากรอกวันเดือนปีเกิด
            .invalid-feedback__desc
        .col-12.form-row
          label.form__label(for='validate-home-address') ที่อยู่ตามทะเบียนบ้าน
          input.form-control#validate-home-address(type='text' placeholder='123 ถ.รัชดาภิเษก ดินแดง กรุงเทพ...' required)
          .valid-feedback 
          .invalid-feedback กรุณากรอกที่อยู่ตามทะเบียนบ้าน
            .invalid-feedback__desc
        .col-12.form-row
          label.form__label(for='validate-address') ที่อยู่ปัจจุบัน
          input.form-control#validate-address(type='text' placeholder='123 ถ.รัชดาภิเษก ดินแดง กรุงเทพ...' required)
          .invalid-feedback กรุณากรอกที่อยู่ปัจจุบัน
            .invalid-feedback__desc
        .col-12.form-row
          label.form__label(for='validate-job') อาชีพ
          select.custom-select.form-control#validate-job(required)
            option 
            option เศรษฐี 
            option Hi-So 
            option พนักงานบริษัทที่หน้าตาดีและมีตังเยอะ 
          .valid-feedback 
          .invalid-feedback กรุณาเลือกอาชีพ
            .invalid-feedback__desc     
        .col-12.form-row
          label.form__label(for='validate-jobaddress') สถานที่ทำงาน
          input.form-control#validate-jobaddress(type='text' placeholder='ชื่อบริษัท, จังหวัด' required)
          .valid-feedback 
          .invalid-feedback  กรุณากรอกที่อยู่ที่ทำงาน
            .invalid-feedback__desc
        .next.form-row
          button.btn.next--btn.btn-secondary#check-invalid(type='submit' disabled) ถัดไป